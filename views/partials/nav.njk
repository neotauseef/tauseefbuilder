<header class="nav" role="banner">
  <div class="nav__inner">
    <a class="nav__brand" href="/" aria-label="{{ site.firm.long_name }} — home">
      <span class="nav__brand-mark">ACCA</span><span class="nav__brand-name">&nbsp;· The Tool for Success</span>
    </a>
    <nav class="nav__links" aria-label="Primary">
      {% for item in site.nav %}
        <a href="{{ item.href }}" class="nav__link{% if current_path == item.href %} is-current{% endif %}">
          <span class="nav__link-mark">{{ item.mark }}</span>
          <span class="nav__link-label">{{ item.label }}</span>
        </a>
      {% endfor %}
    </nav>
  </div>
</header>
